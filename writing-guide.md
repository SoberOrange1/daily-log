# daily-log 写作规范与审查清单

> 配置 agent 时作为 **reference doc** 挂给 daily-log agent(见 quick-set-up.md 第 4 步)。
> 它是 agent 的**写作质量参考**,也是**发布前二轮审查(LLM judge)的评分表**。
> 用法:agent 在 `publish_daily_log` 之前**查阅本文档**,对照它 + 本次 `fetch_events` 原始事件
> 自审 draft,修订后再发布;写作时也据此把关质量。
> (三层结构规则、铁律等"每次必守"的硬约束写在 agent instructions 里,此处不重复。)

---

## 一、核心原则

1. **用用户的话**:沿用源对话里的表达和语气,不用套路改写。
2. **完整逻辑链**:每件事都有 背景(为什么)→ 做了什么 → 结果 →(必要时)下一步。
3. **能量化就量化**:源数据里出现的数字(几个服务 / 几个 bug / 几处改动 / commit 数)照写;没有的不硬造。
4. **如实展示边界**:搞定的体现价值;没搞定的说清卡在哪、为什么、下一步谁推。卡点也是信息,不回避、不美化。
5. **明确下一步**:遗留事项写清当前状态 + 下一步动作(对应 impact_todo)。
6. **格式自然**:summary 用自然成段的中文,不要标签式("做了什么:""结果:")。

## 二、summary 质量标准(daily / weekly 正文)

- 有**明确的进展或过程量**(百分比 / 完成度 / 数量),能量化就量化。
- **不出现无结论的模糊描述**("做了一些优化""处理了若干问题"要具体化)。
- **完整逻辑链**:主线在推进什么、完成了什么、遗留/风险是什么、下一步。
- **daily(要极简)**:`headline` 一句话总主线(≤40字)+ `by_project` 按项目列关键 action/TODO(每项目 ≤3 条、每条 ≤30 字);宁少勿多,不写琐事。
- **weekly**:可分点(进展 / 里程碑 / 遗留 TODO / 风险),每点讲清一件事(在全屏页展开,可略详)。

## 三、发布前二轮审查清单(LLM judge)

生成 draft 后、`publish_daily_log` 前,对照**本次 fetch_events 原始事件**(保真与覆盖)+ 本规范(质量)自审;有问题改一版再发。
- **daily**:轻量自查(过清单,命中就改)。
- **weekly**:完整审查(逐条对照 + 覆盖检查 + high-risk 扫描)。

清单:

1. **保真**:每条 why/change/impact 都能在原始事件里找到依据?有没有编造或过度引申?
2. **覆盖**:今天/本周**重要**的事项有没有被漏掉?(对照原始事件;琐事漏掉没关系)
3. **质量**:是否满足第一、二节?模糊无结论的、缺量化的、每层超 4 条的 → 改。
4. **一致**:没有改写已有的过去日期?`goal_id` 沿用未新造重复?不与过去矛盾?(对照 `get_current_log`)
5. **high-risk 扫描**(见第四节)。

## 四、high-risk 内容处理

扫描疑似**密钥 / 令牌 / 密码 / 个人隐私(PII) / 客户数据 / 内部机密**:
- **不要**把这类原文写进 dashboard 正文或 evidence;必要时**脱敏**(如 `sk-****`)或改为中性描述。
- 在 feed 简报里**标记**"检测到疑似敏感内容(项目 X / 日期 Y),已脱敏/未收录,请确认",交用户处理。
- 拿不准是否敏感时,倾向不收录 + 标记,让用户决定。

## 五、被 @提问 / 交互场景

- **说得散乱**:先听完,别急着整理;按模块复述确认没遗漏再写。
- **不愿给细节**:重逻辑,不强求量化;可提示"有具体数字会更有说服力",但不追问。
- **觉得做得不够**:卡点和挑战也是价值,如实写在解决什么难题,不必凑数。
- **想美化/夸大**:提醒日志目的是如实展示价值与边界,过度美化后续更被动;保持客观。
